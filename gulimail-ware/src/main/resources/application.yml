server:
  port: 7500

spring:
  datasource:
    username: root
    password: zhixuanchang
    url: jdbc:mysql://43.143.216.195:3307/gulimail_wms
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
